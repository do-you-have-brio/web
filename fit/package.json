{
  "name": "fit",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "biome check . --write",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "@google/genai": "^0.12.0",
    "hono": "^4.7.8",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
